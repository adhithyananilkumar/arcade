# 15 — Consolidated Remediation Ledger

Covers this pass (auth smoke test, security fixes, data-layer foundation, contract verification). For the prior architectural-consolidation pass's ledger, see `14_CONSOLIDATION_REPORT.md`.

| ID | Severity | Current state | Root cause | Files | Implementation | Backend dependency | Validation | Remaining risk | Status |
|---|---|---|---|---|---|---|---|---|---|
| `IAM-003` | P0 | Fixed in source | Backend gated 5 admin endpoints on `isAuthenticated()` only; frontend had no guard at all | `ContactMessageController.java`; `app/(authenticated)/console/inbox/page.tsx` | Backend: `@PreAuthorize` on real, existing `platform.system.manage` permission. Frontend: `AuthorizationService.canManageSettings(user)` + `notFound()`, matching every other console page's pattern | None — used an existing seeded permission, no backend permission model change needed | `tsc`, `eslint`, full build, `mvn compile -o` all clean. **Live role-matrix test (anonymous/learner/reviewer/admin) not possible — no running backend.** | Only `PLATFORM_OWNER`/`PLATFORM_ADMIN` can access; if product intends `SUPPORT` role to own this inbox, that role currently has zero permissions seeded and needs a product decision, not invented here | **Fixed in source, unverified live** |
| `XSS-001` | P0 | Fixed in source | Regex-only "sanitization" (`<script>` strip) does nothing against event-handler attributes, `javascript:` URLs, etc. | `domains/community/components/CommentCard.tsx`, `app/(public)/forum/[slug]/page.tsx`, `app/(authenticated)/my-learning/page.tsx` (4th site found this pass, not in original audit), new `shared/utils/sanitizeHtml.ts` | `isomorphic-dompurify` (new dependency) via one shared helper, default allowlist profile + explicit `iframe`/`object`/`embed`/`style` forbid list | None | `tsc`, `eslint`, full build clean. **Live payload testing (script, event-handler, `javascript:` URL, malformed HTML) not possible — no running backend to create/view real forum content.** | DOMPurify is a well-established library; the specific integration (props, `isHtml` flag) wasn't exercised live | **Fixed in source, unverified live** |
| `API-09` | P0 | Fixed in source | Client-supplied `uploadUrl` proxied server-side with zero validation — SSRF primitive | `app/api/internal/media/upload/route.ts` | `assertSafeUploadUrl()`: https-only, rejects literal + DNS-resolved private/loopback/link-local/cloud-metadata IPs (IPv4 + IPv6), `redirect: 'manual'` + reject any redirect response | None — hardening is environment-agnostic, doesn't require knowing the storage bucket's exact hostname | `tsc`, `eslint`, full build clean. **Each named test case (localhost, private ranges, cloud metadata, bad scheme, redirects, malformed URLs, DNS rebinding) traced by hand against the code, not executed live.** | None identified beyond "not live-tested" | **Fixed in source, unverified live** |
| `API-07` | P1 | Already fixed (prior pass), reconfirmed | Hardcoded `localhost:8080` OAuth redirect broke Google login outside local dev | `apps/public/orchestrators/AuthOrchestrator.tsx` | Uses `GOOGLE_OAUTH_URL` from `infrastructure/config/env.ts` (established in the prior consolidation pass) | None | Reconfirmed via `tsc`/build this pass; full OAuth handshake untestable in this sandboxed environment regardless of backend availability | None new | **Already fixed, reconfirmed** |
| `ROUTE-002`/`DEAD-003` | P3 | Fixed and verified | Diagnostic-only route reachable in production with no guard; completion status of its investigation unconfirmed | `app/dev-editor-perf/page.tsx` | `notFound()` guard when `NODE_ENV === 'production'` — reversible choice over deletion, since the investigation's completion couldn't be confirmed | None | **Directly confirmed in production build artifact** (`.next/server/app/dev-editor-perf.meta` shows `status: 404`) — the one item in this pass verified against real build output, not just code review | None | **Fixed and verified** |
| `API-02` | P2 | Fixed | `queryClient.ts`'s retry-skip checked an axios error shape (`error.response.status`) that the canonical `api.ts`'s `ApiError` never has; 401/403s retried 3× pointlessly | `infrastructure/state/queryClient.ts` | Structural `.status` check instead (avoiding a circular import back into `api.ts`) | None | `tsc`, `eslint`, full build clean | None | **Fixed** |
| `CONTRACT-04`-adjacent (new this pass, not a numbered prior finding) | P2 | Fixed | Backend 500 responses can contain a leaked stack trace in `message` (dev-mode config); widespread `toast.error(error.message)` pattern across the app would show it directly to users | `infrastructure/http/api.ts` | Generic message substituted for any `5xx` response at the single central point (`request()`), leaving 4xx messages (user-relevant) untouched; real error still logged to console | The actual leak is backend-side (disabling dev-mode stack-trace inclusion in prod config) — this frontend fix limits blast radius regardless of backend config, but doesn't replace fixing the backend | `tsc`, `eslint`, full build clean; verified by re-reading `request()`'s control flow to confirm the 401-refresh path is untouched | None identified | **Fixed (frontend mitigation); backend-side leak itself out of scope** |
| `IAM-001`/`IAM-002` (re-triage) | P1→ partially resolved | 3 of 6 originally-flagged codes confirmed to already match the backend (not stale); 1 confirmed stale-but-harmless; 1 confirmed genuinely missing; 1 confirmed and fixed | Backend's `IamBootstrap.java` permission model changed since the original audit ran (a recent, documented rename + cleanup) | `infrastructure/auth/authorization.service.ts` | `payments.view` → `platform.payments.view` (real rename, fixed). `platform.roles.assign` left as dead-but-harmless (real `platform.roles.manage` in the same `OR` still works). `platform.permissions.manage` left open — no equivalent exists, not invented. | `platform.permissions.manage` gap requires a product/backend decision | `tsc`, `eslint`, full build clean; permission list re-verified directly against current `IamBootstrap.java` source | `canManagePermissions` remains permanently false for every role except `PLATFORM_OWNER` until a real permission is seeded | **Partially fixed; one gap explicitly documented as open, not invented around** |
| Data layer | — | 2 domains migrated to React Query (`notifications`, `public-categories`); pattern documented | 21 domains had a real API service layer already, but only `community/forum` used React Query — 77 manual-fetch sites identified in the original audit | New: `domains/notifications/api/notification.queries.ts`; rewritten: `domains/notifications/hooks/useNotifications.ts`, `shared/hooks/usePublicCategories.ts` | Query-key registries, `useQuery`/`useMutation`, cache-merge invalidation instead of blanket refetch, `enabled` gating tied to auth status (a real bug caught mid-migration, not hypothetical) | None | `tsc`, `eslint`, full build clean; both hooks' external contracts preserved byte-for-byte, zero consumer changes needed | ~75 domains/call-sites still on the manual-fetch pattern — explicitly deferred, not a regression | **Pattern established; 2 of ~21 domains migrated; remainder deferred to next task per instruction** |

## This pass: learner identity / account / onboarding / profile / activity-streak

| ID | Severity | Current state | Root cause | Files | Implementation | Backend dependency | Validation | Remaining risk | Status |
|---|---|---|---|---|---|---|---|---|---|
| Account lifecycle | — | Verified already correct, hardened | Login had no explicit `isEmailVerified` check (safe only "by construction") | `AuthService.java` | Added explicit rejection of unverified users at login | None | `mvn compile`, new `AuthServiceTest` (1 test, passes) | None — additive hardening only | **Verified + hardened** |
| Username, onboarding persistence | — | Verified already correct, unchanged | N/A — found already production-grade | — | None | None | Re-confirmed via source reading, not re-tested | None | **Verified, no change needed** |
| Interest taxonomy | New feature | Built | Interests were free-text strings (`user_preferences`), not structured/recommendation-ready | `V250__create_interests_schema.sql`, `V255__seed_interests.sql`, `identity/user/interest/*` (backend); `domains/identity/api/interest.*` (frontend); `app/(onboarding)/onboarding/page.tsx` step 4 rewrite | New `Interest`/`LearnerInterest` entities, `/public/interests` + `/me/interests` endpoints, real onboarding UI (search, selection limit, canonical IDs) | None | `mvn compile`, `InterestServiceTest` (3 tests pass), migration dry-run validated against real schema, frontend `tsc`/`eslint`/build clean | Legacy `user_preferences` free-text field left unmigrated (explicitly out of scope) | **Built and validated (compile-level backend, full frontend)** |
| Learning activity / streak | New feature | Built | No backend streak concept existed at all; frontend computed it twice, client-side, from a non-representative signal (`TimeLog`) | `V251`-`V254` migrations, `learning/activity/*` (backend: `LearningActivity`, `LearnerDailyActivity`, `LearnerActivitySummary`, aggregation/query services, `LearningActivityApi` facade); `domains/learning/activity/*` (frontend); `app/(authenticated)/profile/page.tsx` (streak now backend-sourced) | Append-only source of truth → idempotent daily aggregate → rebuildable summary; wired into real triggers (lesson/quiz/course completion); see `LEARNING_ACTIVITY_STREAK.md` for full design | None | `mvn compile`, `LearnerActivityAggregationServiceTest` (6 tests: duplicate handling, consecutive days, missed day, multi-activity-same-day, year boundary — all pass), migration dry-runs validated, frontend `tsc`/`eslint`/build clean | Public-profile page's heatmap still uses the old `TimeLog`-based data (not migrated to the new domain — explicitly scoped to the own-profile page in this pass, documented in the final report); no admin-triggered rebuild endpoint wired yet (service method exists, tested, not exposed via API) | **Built and validated (compile-level backend, full frontend); LIVE/browser verification BLOCKED — see final report** |
| Avatar upload validation | P1-ish hardening | Fixed | No MIME/size/content-sniffing validation existed — any file type/size accepted | `ImageUploadValidator.java`, wired into `UserController.uploadAvatar` | MIME allowlist, 3MB cap, real image decode via `ImageIO` (rejects mislabeled/malformed files), dimension cap | None | `mvn compile`; no dedicated unit test added (validation logic is a thin wrapper around `ImageIO`, judged lower-value to test than the streak algorithm given time) | Not live-tested against a real upload | **Fixed, unit-untested, not live-verified** |
| KYC/Aadhaar removal from profile/settings surface | Explicit requirement | Fixed | `settings/info` page was 100% mock, including fabricated Aadhaar numbers and a simulated proctoring camera, with zero backend wiring | `app/(authenticated)/settings/info/page.tsx` — full rewrite | Rebuilt around only real `User` fields (name, email read-only, phone, gender, address), each field wired to the real `PUT /api/v1/users/me` endpoint | None | `tsc`, `eslint` (fixed a `react-hooks/static-components` issue introduced during the rewrite), full build clean | No KYC backend domain was found to exist anywhere in the codebase — nothing else needed preserving | **Fixed and validated** |
| Profile mock-content cleanup | P0/P1 | Fixed | Fake badges/certificates/achievements arrays (own profile: ~15 badges w/ fake XP+SVG art, 10 fake achievements, 7 fake certificates incl. fabricated credential IDs; public profile: 15 fake badges spliced with a fake streak name) plus a TimeLog-backed heatmap on both pages | `app/(authenticated)/profile/page.tsx` (1900+→~1030 lines), `app/(public)/[username]/page.tsx`, `apps/learner/components/home/LearnerHomePage.tsx`, `StreakCalendar.tsx` | All fake badge/achievement/certificate arrays and their SVG-rendering functions deleted. Own-profile heatmap migrated from TimeLog to the backend-owned `LearnerDailyActivity`/`useDailyActivityQuery`. Home page's client-computed `computeStreak()` replaced with `useActivitySummaryQuery`. Public profile: fake badges + fake-streak splice + TimeLog heatmap removed entirely (no real public equivalent exists — see `PUBLIC_PROFILE_SECURITY.md`) | None new (uses existing `/me/activity/*` endpoints) | `tsc`, `eslint` (unused-import cleanup), full `npm run build` all clean | `AchievementsPage.tsx`'s hardcoded `streakDays={14}` deliberately left untouched — the whole page is mock (badges/certificates arrays) and achievement engine is explicitly out of scope; patching one number would leave a half-real, half-fake page | **Fixed and validated (build-level)** |
| Public per-username TimeLog exposure | P0 (privacy) | Fixed | `GET /api/v1/public/profiles/{username}/activity` was a live, unauthenticated endpoint returning any user's exact per-day time-on-site seconds for 6 months, given only their username | `PublicProfileController.java` (endpoint removed), `UserController.java` (new self-service `GET /me/time-activity`), `TimeLogService.java`, new `TimeLogActivityResponse.java`; frontend: `user.service.ts`, `my-learning/page.tsx`, `[username]/page.tsx` | Endpoint deleted from the public controller; legitimate use (my-learning's own "time spent" chart) migrated to a new self-service, `@AuthenticationPrincipal`-scoped endpoint | None | `mvn compile` clean, `mvn test` (full suite unit-test portion passes — see final report), frontend `tsc`/`eslint`/build clean | None identified | **Fixed and validated (compile + unit-test level)** |

## Pre-existing, unrelated change noticed (not mine, not touched)

`backend/src/main/java/com/arcade/backend/infrastructure/storage/controller/StorageController.java` has a 1-line uncommitted change that predates this session (last commit to that file was hours before this task began). Left untouched — not part of this task's scope and not something I introduced.

## Cleanup performed

Removed my own backend startup log files (`backend-out*.log`, `backend-err*.log`) generated while diagnosing the JDK loopback-connection issue — debugging artifacts, not deliverables.
